version: '3.8'

services:
  ai-service:
    image: ghcr.io/${GITHUB_USERNAME}/hearrt-ai:${AI_VERSION:-latest}
    restart: always
    env_file:
      - .env
    ports:
      - "${HOST_PORT:-8000}:8000"
    networks:
      - hearrt_network

networks:
  hearrt_network:
    external: true
